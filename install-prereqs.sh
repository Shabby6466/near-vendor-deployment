#!/bin/bash
#
# This script installs prerequisites for the NearVendor deployment on a
# fresh Debian-based Linux server (e.g., Ubuntu).
# Run with sudo: sudo sh ./install-prereqs.sh
#
echo "### Installing prerequisites for NearVendor Deployment ###"
apt-get update
apt-get install -y ca-certificates curl gnupg
install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg
chmod a+r /etc/apt/keyrings/docker.gpg
echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu \
  $(. /etc/os-release && echo "$VERSION_CODENAME") stable" | \
  tee /etc/apt/sources.list.d/docker.list > /dev/null
apt-get update
apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
echo "### Docker and Docker Compose installed successfully. ###"
exit 0
